<Window x:Class="ScadaGUI.AddTagWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ScadaGUI"
        mc:Ignorable="d"
        Title="Dodaj" Height="650" Width="600" 
        WindowStartupLocation="CenterOwner"
        Background="#0A0A0A" 
        Foreground="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Dodaj Novi Objekat" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="Tip: " VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,15,0"/>
            <RadioButton x:Name="RbAI" Content="AI" GroupName="TagType" IsChecked="True" Checked="RadioButton_Checked" Margin="0,0,15,0" Foreground="White"/>
            <RadioButton x:Name="RbAO" Content="AO" GroupName="TagType" Checked="RadioButton_Checked" Margin="0,0,15,0" Foreground="White"/>
            <RadioButton x:Name="RbDI" Content="DI" GroupName="TagType" Checked="RadioButton_Checked" Margin="0,0,15,0" Foreground="White"/>
            <RadioButton x:Name="RbDO" Content="DO" GroupName="TagType" Checked="RadioButton_Checked" Margin="0,0,15,0" Foreground="White"/>
            <RadioButton x:Name="RbAlarm" Content="Alarm" GroupName="TagType" Checked="RadioButton_Checked" Foreground="White"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="Ime Taga/Alarma:"/>
            <TextBox x:Name="TxtName" Margin="0,5,0,10" Height="25"/>

            <TextBlock x:Name="LblAddress" Text="I/O Adresa:"/>
            <TextBox x:Name="TxtAddress" Margin="0,5,0,10" Height="25"/>

            <TextBlock Text="Opis:"/>
            <TextBox x:Name="TxtDescription" Margin="0,5,0,10" Height="25"/>
        </StackPanel>

        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
            <Grid>
                <StackPanel x:Name="PanelAI" Visibility="Visible">
                    <TextBlock Text="Donja granica (Low Limit):"/>
                    <TextBox x:Name="TxtAiLowLimit" Margin="0,5,0,10" Height="25" Text="0"/>
                    <TextBlock Text="Gornja granica (High Limit):"/>
                    <TextBox x:Name="TxtAiHighLimit" Margin="0,5,0,10" Height="25" Text="100"/>
                    <TextBlock Text="Jedinica (Units):"/>
                    <TextBox x:Name="TxtAiUnits" Margin="0,5,0,10" Height="25" Text="°C"/>
                    <TextBlock Text="Period skeniranja (s):"/>
                    <TextBox x:Name="TxtAiScanTime" Margin="0,5,0,10" Height="25" Text="1"/>
                    <CheckBox x:Name="ChkAiOnScan" Content="On scan" IsChecked="True" Foreground="White" Margin="0,5,0,10"/>
                    <TextBlock Text="Deadband:"/>
                    <TextBox x:Name="TxtAiDeadband" Margin="0,5,0,10" Height="25" Text="0"/>
                    <TextBlock Text="Hysteresis:"/>
                    <TextBox x:Name="TxtAiHysteresis" Margin="0,5,0,10" Height="25" Text="0"/>
                </StackPanel>

                <StackPanel x:Name="PanelAO" Visibility="Collapsed">
                    <TextBlock Text="Početna vrednost (Initial Value):"/>
                    <TextBox x:Name="TxtAoInitValue" Margin="0,5,0,10" Height="25" Text="0"/>
                </StackPanel>

                <StackPanel x:Name="PanelDI" Visibility="Collapsed">
                    <TextBlock Text="Period skeniranja (s):"/>
                    <TextBox x:Name="TxtDiScanTime" Margin="0,5,0,10" Height="25" Text="1"/>
                    <CheckBox x:Name="ChkDiOnScan" Content="On scan" IsChecked="True" Foreground="White" Margin="0,5,0,10"/>
                </StackPanel>

                <StackPanel x:Name="PanelDO" Visibility="Collapsed">
                    <TextBlock Text="Početna vrednost (0 ili 1):"/>
                    <TextBox x:Name="TxtDoInitValue" Margin="0,5,0,10" Height="25" Text="0"/>
                </StackPanel>

                <StackPanel x:Name="PanelAlarm" Visibility="Collapsed">
                    <TextBlock Text="Limit vrednost:"/>
                    <TextBox x:Name="TxtAlarmLimit" Margin="0,5,0,10" Height="25"/>
                    <TextBlock Text="Smer alarma:"/>
                    <ComboBox x:Name="CbDirection" Margin="0,5,0,10" Height="25">
                        <ComboBoxItem Content="Above" IsSelected="True"/>
                        <ComboBoxItem Content="Below"/>
                    </ComboBox>
                    <TextBlock Text="Poruka alarma:"/>
                    <TextBox x:Name="TxtAlarmMessage" Margin="0,5,0,10" Height="25"/>
                    <TextBlock Text="AI tag:"/>
                    <ComboBox x:Name="CbTagList" DisplayMemberPath="Name" SelectedValuePath="Name" Margin="0,5,0,10" Height="25"/>
                </StackPanel>
            </Grid>
        </ScrollViewer>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Sačuvaj" Click="SaveButton_Click" Width="100" Height="30" Margin="0,0,10,0" Background="LimeGreen" Foreground="Black" FontWeight="Bold" IsDefault="True"/>
            <Button Content="Otkaži" IsCancel="True" Width="100" Height="30" Background="Gray" Foreground="White"/>
        </StackPanel>
    </Grid>
</Window>